* text=auto

# these must always stay LF on every OS (docker build + container exec)
*.sh    text eol=lf
Dockerfile text eol=lf
Makefile   text eol=lf
*.patch    text eol=lf
*.dockerignore text eol=lf
